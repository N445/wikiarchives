{% extends 'base.html.twig' %}

{% block body %}
    {% include 'includes/banner.html.twig' with {banner_title:'homepage.title'|trans([],'front')} %}
    <div class="container-fluid px-4">
        <div class="row row-cols-1 row-cols-md-5 g-5 my-3">
            {% if root %}
                {% for catalog in root.children %}
                    <div class="col">
                        {% include 'includes/catalog/_catalog-card.html.twig' %}
                    </div>
                {% endfor %}
            {% endif %}
        </div>
    </div>
{% endblock %}
