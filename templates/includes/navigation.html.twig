<nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top">
    <div class="container">
        <a class="navbar-brand" href="{{ path('HOMEPAGE') }}">Wikiarchives</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('HOMEPAGE') }}">
                        {{ 'navbar.homepage'|trans([],'front') }}
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('ABOUT') }}">
                        {{ 'navbar.about'|trans([],'front') }}
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('LEGAL_MENTION') }}">
                        {{ 'navbar.legal_mention'|trans([],'front') }}
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('MAP') }}">
                        {{ 'navbar.map'|trans([],'front') }}
                    </a>
                </li>
            </ul>
            <form action="{{ path('SEARCH') }}" class="d-flex">
                <div class="input-group">
                    <input class="form-control" type="search" placeholder="{{ 'actions.search_placeholder'|trans([],'front') }}" aria-label="Search" name="q"
                           value="{{ query??'' }}">
                    <button class="btn btn-wikiarchives-primary" type="submit">{{ 'actions.search_submit'|trans([],'front') }}</button>
                </div>
            </form>
            <div class="login ms-3">
                {% if app.user %}
                    <div class="btn-group">
                        <a href="{{ path('PROFILE') }}" type="button" class="btn btn-outline-wikiarchives-primary">
                            {{ app.user.userIdentifier }}
                        </a>
                        <button type="button" class="btn btn-wikiarchives-primary dropdown-toggle dropdown-toggle-split"
                                data-bs-toggle="dropdown" aria-expanded="false">
                            <span class="visually-hidden">Dropdown</span>
                        </button>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li><a class="dropdown-item" href="{{ path('PROFILE') }}">{{ 'actions.profile'|trans([],'front') }}</a></li>
                            <li><a class="dropdown-item" href="{{ path('APP_LOGOUT') }}">{{ 'actions.logout'|trans([],'front') }}</a></li>
                        </ul>
                    </div>

                {% else %}
                    <div class="btn-group">
                        <a href="{{ path('APP_LOGIN') }}" type="button" class="btn btn-outline-wikiarchives-primary">{{ 'actions.login'|trans([],'front') }}</a>
                        <button type="button" class="btn btn-wikiarchives-primary dropdown-toggle dropdown-toggle-split"
                                data-bs-toggle="dropdown" aria-expanded="false">
                            <span class="visually-hidden">Toggle Dropdown</span>
                        </button>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li><a class="dropdown-item" href="{{ path('APP_REGISTER') }}">{{ 'actions.sign_up'|trans([],'front') }}</a></li>
                        </ul>
                    </div>
                {% endif %}
            </div>
            <ul class="navbar-nav">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" id="navbarDropdown" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        {{ app.request.locale|getLocalName|capitalize }}
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                        {#                <li><a class="dropdown-item" href="#!">Settings</a></li>#}
                        {#                <li><a class="dropdown-item" href="#!">Activity Log</a></li>#}
                        {#                <li><hr class="dropdown-divider" /></li>#}
                        <li><a class="dropdown-item" href="{{ path('LOCALE_SWITCH',{'_locale':'en'}) }}">English</a></li>
                        <li><a class="dropdown-item" href="{{ path('LOCALE_SWITCH',{'_locale':'fr'}) }}">Français</a></li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>