{% extends 'base-admin.html.twig' %}

{% block title %}Catalog{% endblock %}

{% block body %}
    <div class="container">
        <h1>Catalog</h1>

        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ catalog.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ catalog.name }}</td>
            </tr>
            <tr>
                <th>Lft</th>
                <td>{{ catalog.lft }}</td>
            </tr>
            <tr>
                <th>Lvl</th>
                <td>{{ catalog.lvl }}</td>
            </tr>
            <tr>
                <th>Rgt</th>
                <td>{{ catalog.rgt }}</td>
            </tr>
            </tbody>
        </table>

        <a href="{{ path('ADMIN_CATALOG_CATALOG_INDEX') }}">back to list</a>

        <a href="{{ path('ADMIN_CATALOG_CATALOG_EDIT', {'id': catalog.id}) }}">edit</a>

        {{ include('catalog/catalog/_delete_form.html.twig') }}
    </div>
{% endblock %}
