{% extends 'base-admin.html.twig' %}

{% block title %}Modification de "{{ picture.name }}" - {{ parent() }}{% endblock %}
{% block bodytitle %}Modification de "{{ picture.name }}"{% endblock %}

{% block body %}
    <div class="d-flex mb-3">
        <a href="{{ path('ADMIN_CATALOG_PICTURE_INDEX_CATALOG',{'catalogId':picture.catalog.id}) }}" class="btn btn-primary me-3">
            Revenir au listing
        </a>

        {{ include('catalog/picture/_delete_form.html.twig') }}
    </div>

    {{ include('catalog/picture/_form.html.twig', {'button_label': 'Modifier'}) }}
{% endblock %}

{% block stylesheets %}
    {{ encore_entry_link_tags('place-map-form') }}
{% endblock %}

{% block javascripts %}
    {{ encore_entry_script_tags('wysiwyg-js') }}
    {{ encore_entry_script_tags('place-map-formjs') }}
{% endblock %}
