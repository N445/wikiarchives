<form method="post" action="{{ path('ADMIN_CATALOG_CATALOG_DELETE', {'id': catalog.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ catalog.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
