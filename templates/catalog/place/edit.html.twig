{% extends 'base-admin.html.twig' %}

{% block title %}Modification de la place {{ place.name }} - {{ parent() }}{% endblock %}

{% block body %}
    <div class="container">
        <h1>Modification de la place {{ place.name }}</h1>

        {{ include('catalog/place/_form.html.twig', {'button_label': 'Modifier'}) }}

        <a href="{{ path('ADMIN_CATALOG_PLACE_INDEX') }}" class="btn btn-primary my-3">Revenir au listing</a>

        {{ include('catalog/place/_delete_form.html.twig') }}
    </div>
{% endblock %}
