{% extends 'base-admin.html.twig' %}

{% block title %}{{ 'actions.modification_of'|trans([],'admin') }} "{{ catalog.name }}" - {{ parent() }}{% endblock %}
{% block bodytitle %}{{ 'actions.modification_of'|trans([],'admin') }} "{{ catalog.name }}"{% endblock %}

{% block body %}
    {{ include('catalog/catalog/_form.html.twig', {'button_label': 'actions.edit'}) }}

{#    <a href="{{ path('ADMIN_CATALOG_CATALOG_BROWSE',{'id':catalog.id}) }}" class="btn btn-primary mb-3">#}
{#        {{ 'actions.back_listing'|trans([],'admin') }}#}
{#    </a>#}

    {{ include('catalog/catalog/_delete_form.html.twig') }}
{% endblock %}

{% block javascripts %}
    {{ encore_entry_script_tags('wysiwyg-js') }}
{% endblock %}