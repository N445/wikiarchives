{% extends 'base-admin.html.twig' %}

{% block title %}{{ 'actuality.edit'|trans([],'admin') }} - {{ parent() }}{% endblock %}
{% block bodytitle %}{{ 'actuality.edit'|trans([],'admin') }} - {{ actuality.title }}{% endblock %}

{% block body %}

    {{ include('actuality/actuality/_form.html.twig', {'button_label': 'actions.edit'}) }}

    <a href="{{ path('ACTUALITY_ACTUALITY_INDEX') }}">{{ 'actions.back_listing'|trans([],'admin') }}</a>

    {{ include('actuality/actuality/_delete_form.html.twig') }}
{% endblock %}

{% block javascripts %}
    {{ encore_entry_script_tags('wysiwyg-js') }}
{% endblock %}