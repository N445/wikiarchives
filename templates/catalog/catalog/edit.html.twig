{% extends 'base-admin.html.twig' %}

{% block title %}Modifier catalogue {{ catalog.name}} - {{ parent() }}{% endblock %}

{% block body %}
<div class="container">
    <h1>Modifier catalogue {{ catalog.name}}</h1>

    {{ include('catalog/catalog/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('ADMIN_CATALOG_CATALOG_INDEX') }}" class="btn btn-primary mb-3">Revenir au listing</a>

    {{ include('catalog/catalog/_delete_form.html.twig') }}
</div>
{% endblock %}
