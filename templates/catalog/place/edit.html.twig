{% extends 'base-admin.html.twig' %}

{% block title %}Modification de la zone {{ place.name }} - {{ parent() }}{% endblock %}
{% block bodytitle %}Modification de la zone {{ place.name }}{% endblock %}

{% block body %}
        {{ include('catalog/place/_form.html.twig', {'button_label': 'actions.edit'}) }}

        <a href="{{ path('ADMIN_CATALOG_PLACE_INDEX') }}" class="btn btn-primary my-3">{{ 'actions.back_listing'|trans([],'admin') }}</a>

        {{ include('catalog/place/_delete_form.html.twig') }}
{% endblock %}

{% block stylesheets %}
        {{ encore_entry_link_tags('place-map-form') }}
{% endblock %}

{% block javascripts %}
        {{ encore_entry_script_tags('place-map-formjs') }}
{% endblock %}